scanDirs - std::vector<std::string> директории для сканирования (может быть несколько)
skipDirs - std::vector<std::string> директории для исключения из сканирования (может быть несколько)
scanLevel - int уровень сканирования (один на все директории, 0 - только указанная
директория без вложенных).
minFileSize - int минимальный размер файла, по умолчанию проверяются все файлы
больше 1 байта.
fileMasks - std::string маски имен файлов разрешенных для сравнения (не зависят от
регистра), пример - .txt|.h .
hashType - std::string -один из имеющихся алгоритмов хэширования (crc32, sha1), по умолчанию crc32.
blockSize - int - размер блока, которым производится чтения файлов. 

--scanDirs D:\tests\Duplicates --scanLevel 2 --blockSize 1024000  --fileMasks dump|.h

